import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.controller.zammad',
  appName: 'SOS CONTROLLER',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
